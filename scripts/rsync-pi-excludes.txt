# Canonical Pi payload excludes — used by:
#   scripts/rsync-pi-payload.sh   (junk filters within allowlisted paths)
#   scripts/prune-pi-payload.sh   (after sync / git clone fallback)
#   scripts/verify-pi-payload.sh  (post-sync / post-provision gate)
#
# Dev sync and provision --src both use the allowlist in
# scripts/rsync-pi-includes.txt via scripts/rsync-pi-payload.sh.
#
# Runtime paths that MUST remain: piwallet/, scripts/, deploy/, pyproject.toml
#
# KEEP_ON_PI: .venv / venv / env are listed below so rsync does not copy a
# Mac venv to the Pi, but prune + verify leave the Pi-local .venv alone.

# VCS & IDE
.git
.github
.vscode
.idea
.DS_Store

# Python env & build artifacts
.venv
venv
env
__pycache__
*.pyc
*.pyo
*.egg-info
build
dist
.pytest_cache
.ruff_cache
.mypy_cache
htmlcov
.coverage
.tox

# Node
node_modules

# Local vault / secrets — never push dev machine state to the Pi
# (vault normally lives in ~/.piwallet, outside the repo tree)
.piwallet
.piwallet-dev
vault.bin
vault.bin.bak
*.vault
local_*.cbor
scratch

# Captured SD images — workstation only (publish via GitHub Releases)
images

# Not needed on the Pi (bonnet runtime / image build)
companion
hardware
docs
tests
releases
site
_site
mkdocs.yml
requirements-docs.txt
enclosure

# Workstation-only helpers
deploy.sh
